---
name: RAGFlow 分层设计
category: 架构设计
source: api/ragflow_server.py
---

## 描述

RAGFlow 采用经典的五层架构设计：用户层、接入层、API 层、服务层、引擎层和数据层。每层职责明确，通过标准接口通信。用户层提供多端访问；接入层处理负载均衡和安全；API 层实现路由和认证；服务层封装业务逻辑；引擎层提供核心能力；数据层统一管理所有数据存储。

## 分层架构图

```mermaid
flowchart TB
    subgraph L1["层 1: 用户层 (Presentation Layer)"]
        WEB[Web UI<br/>React + TypeScript]
        SDK[API SDK<br/>Python]
        CLI[CLI Tools<br/>Python]
    end

    subgraph L2["层 2: 接入层 (Access Layer)"]
        NGINX[Nginx<br/>反向代理<br/>负载均衡<br/>SSL 终止<br/>静态资源服务]
    end

    subgraph L3["层 3: API 层 (Application Layer)"]
        API[Quart Server<br/>蓝图路由系统<br/>RESTful API<br/>WebSocket 支持<br/>认证中间件]
        BP1[kb_app]
        BP2[dialog_app]
        BP3[canvas_app]
        BP4[document_app]
    end

    subgraph L4["层 4: 服务层 (Service Layer)"]
        SVC[业务逻辑服务<br/>26 个服务类]
        S1[KnowledgebaseService]
        S2[DocumentService]
        S3[DialogService]
        S4[CanvasService]
        S5[LLMService]
        S6[UserService]
    end

    subgraph L5["层 5: 引擎层 (Engine Layer)"]
        RAG[RAG Engine<br/>文档解析<br/>文本分块<br/>向量化<br/>检索<br/>LLM 调用<br/>GraphRAG]
        AGT[Agent Engine<br/>Canvas 执行引擎<br/>组件系统<br/>变量引用<br/>流程控制<br/>事件流<br/>工具集成]
    end

    subgraph L6["层 6: 数据层 (Data Layer)"]
        DB[(MySQL<br/>元数据)]
        CACHE[(Redis<br/>缓存)]
        ES[(ES/Infinity<br/>向量)]
        FILES[(MinIO<br/>文件)]
    end

    WEB --> NGINX
    SDK --> NGINX
    CLI --> NGINX

    NGINX --> API
    API --> BP1
    API --> BP2
    API --> BP3
    API --> BP4

    BP1 --> S1
    BP2 --> S3
    BP3 --> S4
    BP4 --> S2

    S1 --> RAG
    S2 --> RAG
    S3 --> RAG
    S4 --> AGT
    S5 --> RAG
    S5 --> AGT
    S6 --> DB

    RAG --> DB
    RAG --> CACHE
    RAG --> ES
    RAG --> FILES

    AGT --> CACHE
    AGT --> DB

    style WEB fill:#1a73e8,stroke:#0d47a1,color:#ffffff
    style SDK fill:#1a73e8,stroke:#0d47a1,color:#ffffff
    style CLI fill:#1a73e8,stroke:#0d47a1,color:#ffffff
    style NGINX fill:#f57c00,stroke:#e65100,color:#ffffff
    style API fill:#43a047,stroke:#1b5e20,color:#ffffff
    style BP1 fill:#66bb6a,stroke:#2e7d32,color:#ffffff
    style BP2 fill:#66bb6a,stroke:#2e7d32,color:#ffffff
    style BP3 fill:#66bb6a,stroke:#2e7d32,color:#ffffff
    style BP4 fill:#66bb6a,stroke:#2e7d32,color:#ffffff
    style SVC fill:#7e57c2,stroke:#4527a0,color:#ffffff
    style S1 fill:#9575cd,stroke:#5e35b1,color:#ffffff
    style S2 fill:#9575cd,stroke:#5e35b1,color:#ffffff
    style S3 fill:#9575cd,stroke:#5e35b1,color:#ffffff
    style S4 fill:#9575cd,stroke:#5e35b1,color:#ffffff
    style S5 fill:#9575cd,stroke:#5e35b1,color:#ffffff
    style S6 fill:#9575cd,stroke:#5e35b1,color:#ffffff
    style RAG fill:#ec407a,stroke:#ad1457,color:#ffffff
    style AGT fill:#ec407a,stroke:#ad1457,color:#ffffff
    style DB fill:#ffa726,stroke:#ef6c00,color:#000000
    style CACHE fill:#ffa726,stroke:#ef6c00,color:#000000
    style ES fill:#ffa726,stroke:#ef6c00,color:#000000
    style FILES fill:#ffa726,stroke:#ef6c00,color:#000000
```

## 各层职责

### 层 1: 用户层

提供多种访问方式：

| 组件 | 技术栈 | 职责 |
|------|--------|------|
| Web UI | React + TypeScript | 主要交互界面 |
| API SDK | Python | 编程接口 |
| CLI Tools | Python | 命令行工具 |

### 层 2: 接入层

处理外部流量：

- **Nginx**: 反向代理、负载均衡、SSL 终止
- **静态资源**: 前端文件服务

### 层 3: API 层

实现业务接口：

- **Quart Server**: 异步 Web 框架
- **蓝图系统**: 16 个功能蓝图
- **RESTful API**: 7 个 API 模块
- **认证**: JWT Token 验证

### 层 4: 服务层

封装业务逻辑：

| 服务 | 职责 |
|------|------|
| KnowledgebaseService | 知识库管理 |
| DocumentService | 文档处理 |
| DialogService | 对话管理 |
| CanvasService | 画布管理 |
| LLMService | LLM 配置 |

### 层 5: 引擎层

核心能力实现：

- **RAG Engine**: 文档解析、检索、生成
- **Agent Engine**: 工作流编排、执行

### 层 6: 数据层

统一数据存储：

| 存储 | 用途 |
|------|------|
| MySQL | 元数据、用户数据 |
| Redis | 缓存、会话、队列 |
| Elasticsearch | 向量、全文检索 |
| MinIO | 文件、对象存储 |

## 层间通信

```mermaid
sequenceDiagram
    participant U as 用户层
    participant A as 接入层
    participant API as API 层
    participant S as 服务层
    participant E as 引擎层
    participant D as 数据层

    U->>A: HTTP/WS 请求
    A->>API: 转发请求
    API->>S: 函数调用
    S->>E: 函数调用
    E->>D: 数据操作
    D-->>E: 返回数据
    E-->>S: 返回结果
    S-->>API: 返回结果
    API-->>A: HTTP 响应
    A-->>U: 返回响应

    Note over U,D 请求向下流动<br/>响应向上返回
```

## 设计原则

```mermaid
mindmap
  root((架构设计原则))
    单向依赖
      上层依赖下层
      下层不依赖上层
      避免循环依赖
    接口隔离
      标准接口通信
      明确定义契约
      版本化管理
    职责单一
      每层职责明确
      高内聚低耦合
      易于测试维护
    可替换性
      层内实现可替换
      不影响其他层
      支持多种实现
```

## 相关文档

- [001-整体架构.md](./001-整体架构.md) - 整体架构概览
- [003-数据流.md](./003-数据流.md) - 数据流转
- [02-API层/001-Flask服务器.md](../02-API层/001-Flask服务器.md) - API 层详解
- [02-API层/004-服务层.md](../02-API层/004-服务层.md) - 服务层详解

## 相关文件

- [`api/ragflow_server.py`](../../api/ragflow_server.py) - API 层入口
- [`api/apps/__init__.py`](../../api/apps/__init__.py) - 蓝图注册
- [`api/db/services/`](../../api/db/services/) - 服务层实现
- [`rag/`](../../rag/) - RAG 引擎
- [`agent/`](../../agent/) - Agent 引擎
- [`deepdoc/parser/`](../../deepdoc/parser/) - 文档解析器
